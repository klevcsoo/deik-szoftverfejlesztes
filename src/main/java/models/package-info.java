/**
 * The models package contains the model classes that represent the data and business logic of the
 * application. These classes encapsulate the state and behavior of the application, and are
 * responsible for managing the data, performing calculations, and enforcing business rules.
 */
package models;
